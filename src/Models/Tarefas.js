export default class Tarefas {
    constructor (descricao, ano, prioridade, situacao) {
        
        this.descricao = descricao
        this.ano = ano
        this.prioridade = prioridade
        this.situacao = situacao
    }
}